# Brand reference images

- `haxbyte-mark-source.png` — icon only (brackets + circuit-trace mark)
- `haxbyte-logo-source.png` — full lockup (mark + "haxbyte" wordmark + tagline)

Cropped from `ChatGPT Image Aug 10, 2026, 05_17_19 PM.png`, panel 04 of a 5-up DALL-E
concept grid — the design Doug picked. These are the accurate current reference; they
supersede the older hand-authored SVGs (`haxbyte-icon.svg`, `haxbyte-logo.svg`, etc.) which
don't match this concept. See [`../../open-decisions.md`](../../open-decisions.md) for the
open item to redraw those SVGs as flat vectors from this reference, and why auto-tracing
won't work cleanly (source is a stylized 3D render, not flat art).

A mirror of these files also lives in the `career-development` repo at
`projects/haxbyte-assets/` alongside the original (inaccurate) SVGs and a fuller writeup —
this copy is here so the site repo is self-contained and doesn't depend on a sibling repo
path. If these ever get regenerated or re-cropped, update both copies.
